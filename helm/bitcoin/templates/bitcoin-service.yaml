apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-bitcoin-service
spec:
  ports:
  - port: {{ include "bitcoin.rpcPort" . }}
    protocol: TCP
    targetPort: {{ include "bitcoin.rpcPort" . }}
  selector:
    app: bitcoin
    release: {{ .Release.Name }}